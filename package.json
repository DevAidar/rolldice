{
	"name": "roll-dice",
	"homepage": "https://rolldice.devaidar.com/",
	"version": "0.1.0",
	"private": true,
	"proxy": "http://localhost:5000",
	"dependencies": {
		"@hapi/joi": "^17.1.1",
		"bcryptjs": "^2.4.3",
		"body-parser": "^1.19.0",
		"cors": "^2.8.5",
		"dotenv": "^8.2.0",
		"drei": "^1.5.2",
		"express": "^4.17.1",
		"helmet": "^4.1.1",
		"jsonwebtoken": "^8.5.1",
		"localenv": "^0.2.2",
		"mongoose": "^5.10.7",
		"morgan": "^1.10.0",
		"multer": "^1.4.2"
	},
	"scripts": {
		"old-start": "cd client && react-scripts start",
		"start": "node server.js",
		"dev": "yarn run heroku-postbuild && nodemon server.js",
		"build": "react-scripts build",
		"test": "react-scripts test",
		"eject": "react-scripts eject",
		"install-client": "cd client && yarn",
		"heroku-postbuild": "yarn run install-client && cd client && yarn build"
	},
	"engines": {
		"node": ">=10.6.0"
	},
	"browserslist": {
		"production": [
			">0.2%",
			"not dead",
			"not op_mini all"
		],
		"development": [
			"last 1 chrome version",
			"last 1 firefox version",
			"last 1 safari version"
		]
	},
	"devDependencies": {
		"babel-eslint": "^10.1.0",
		"eslint-plugin-node": "^11.1.0",
		"nodemon": "^2.0.4"
	}
}
